import type { Metadata } from "next";

export const metadata: Metadata={
    title:'Setings'
}

export default function SettingsPage(){
    return (
        <div>
            Settings Page
        </div>
    )
}